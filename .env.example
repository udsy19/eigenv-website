# Supabase — form submissions + PDF uploads.
# Without these the contact form falls back to composing a mailto, so the site
# still works. Project settings → API.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Set these as Edge Function secrets in Supabase (NOT here / NOT NEXT_PUBLIC):
#   RESEND_API_KEY, ALERT_TO, ALERT_FROM, WEBHOOK_SECRET
